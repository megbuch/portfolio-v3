// Expandable cell. Initially, displays as a banner image with a title overlay
// When clicked, bottom expands/slides out to display more info.

export default function ProjectCell() {
  return (
    <div id="project-cell-comp">
    </div>
  )
}
